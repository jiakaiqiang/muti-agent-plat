# 群聊附件、Skill 与 Agent 协同 SDD 实施路线图 v1

> 状态：按严格串行门禁实施。GC-N 的 spec/plan/task/checklist 和验证证据全部完成后，才能启动 GC-(N+1)。

## SDD 四件套

每个任务必须同时具备 spec、plan、task、checklist；文档存在不等于代码验收完成。

## 任务总览

| ID | 任务 | 前置 | 四件套 |
| --- | --- | --- | --- |
| GC-01 | 共享消息、Tag 与附件合同 | 无 | t01 |
| GC-02 | 输入框附件与 Tag 状态 | GC-01 | t02 |
| GC-03 | 附件上传、校验与持久化 | GC-01 | t03 |
| GC-04 | 图片内容理解与重试 | GC-03 | t04 |
| GC-05 | 文件按需读取与上下文引用 | GC-03 | t05 |
| GC-06 | Skill 注册中心与分层治理 | GC-01 | t06 |
| GC-07 | `/Skill` 分类选择器 | GC-02、GC-06 | t07 |
| GC-08 | `@Agent` 列表与加入确认 | GC-01 | t08 |
| GC-09 | Tag 序列化与语义路由 | GC-01、GC-04、GC-05、GC-06、GC-08 | t09 |
| GC-10 | 协同任务面板与阶段输出 | GC-09 | t10 |
| GC-11 | 取消、失败重试与重新汇总 | GC-10 | t11 |
| GC-12 | 历史、生命周期与删除一致性 | GC-03、GC-08、GC-09 | t12 |
| GC-13 | 权限、失败回退与安全边界 | GC-03、GC-06、GC-08、GC-09 | t13 |
| GC-14 | 端到端验收与追踪矩阵 | GC-01–GC-13 | t14 |

## 当前执行状态

- [x] GC-01 至 GC-10：已完成并通过各自四件套门禁。
- [x] GC-11：取消、失败重试与重新汇总已完成，验证证据已回填。
- [x] GC-12：消息/群聊删除、附件失效、历史引用重解析和临时 Agent 关系清理已完成；Server 全量测试 1680 通过、17 跳过。
- [x] GC-13：附件、Skill、Agent、消息路由和历史上下文的服务端 fail-closed 加固已完成；安全负向测试、定向回归和全量服务端门禁已通过（2026-09-22）。
- [x] GC-14：端到端验收与追踪矩阵已完成；全局 typecheck/test/harness/build 和代表性 E2E 已记录，两条专项隔离/协调路由夹具已补跑通过，仅外部模型、外部 PostgreSQL 和 Windows 原生缩放明确标记 pending（2026-09-22）。

## 串行执行顺序

```text
GC-01 → GC-02 → GC-03 → GC-04 → GC-05 → GC-06 → GC-07
  → GC-08 → GC-09 → GC-10 → GC-11 → GC-12 → GC-13 → GC-14
```

## 统一完成门槛

1. 四件套存在并互相链接。
2. 代码变更只覆盖任务范围。
3. 至少一项可复现的自动化或手工验证通过。
4. 失败、跳过和外部依赖如实记录。
5. 完成后同步更新 checklist 证据和本路线图状态。
