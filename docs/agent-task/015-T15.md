# T15 架构查看意图失败测试

- 状态：completed
- 预计时间：10 分钟
- 阶段：C
- 依赖：T14

## 目标

为“查看当前项目的架构”添加回归测试。

## 实施范围

apps/server/src/modules/intent-recognition；apps/server/src/modules/orchestrator

## 测试

运行 IntentRecognition 单测并确认修改前失败。

## 验收标准

- 测试精确覆盖真实用户输入。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增精确使用真实输入“查看当前项目的架构”的 IntentRecognition 回归测试，断言任务意图必须为 `analysis`。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\intent-recognition\\intent-recognition.service.spec.ts`（在 `apps/server` 下执行）；修改前结果为 6 项中 5 项通过、1 项按预期失败，实际 `implementation`、期望 `analysis`。
- 备注：本任务只建立失败测试，没有修改意图识别实现；规则修复严格留给依赖任务 T16。
