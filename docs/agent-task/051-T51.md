# T51 WorkspaceProvider 接口

- 状态：pending
- 预计时间：15 分钟
- 阶段：G
- 依赖：T50

## 目标

创建统一 WorkspaceProvider 接口。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行 server typecheck。

## 验收标准

- Provider 方法签名固定。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-provider.contract.spec.ts` 1/1 通过
- 备注：
  - 在 `packages/shared/src/contracts.ts` 新增 `ApplyChangeSetResult` 判别联合，覆盖成功（`ok:true` + `changeSetId`/`revision`/`appliedCount`）与冲突（`ok:false` + `conflicts: WorkspaceConflictError[]`）两种返回形态。
  - 新增 `apps/server/src/modules/workspaces/workspace-provider.ts`，`WorkspaceProvider` 接口暴露 `kind` 与 `capabilities`/`getRevision`/`listDirectory`/`statFile`/`readFile`/`searchText`/`applyChangeSet` 七个固定签名，全部复用 T45–T50 的共享合同类型。

