# T65 Index 推导入口

- 状态：pending
- 预计时间：15 分钟
- 阶段：I
- 依赖：T64

## 目标

根据路径和配置推导入口候选。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 Index 单测。

## 验收标准

- main、package、README 被识别。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/derive-workspace-entrypoints.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/derive-workspace-entrypoints.ts`，`deriveWorkspaceEntrypoints(snapshot, index)` 返回排序后的候选入口路径列表。
  - 识别 4 类候选：`README(.*)`、`package.json`、常见入口 basename（`index.*`/`main.*`/`server.*`）、以及 `package.json` 的 `main`/`module`/`browser`/`bin`（字符串或对象）解析出的相对路径（相对 `package.json` 的目录归一化）。
  - 过滤已被 `generated:true` 或 `sensitive:true` 的条目，`dist/index.js`、`.env` 等即使匹配基础模式也不会被判为入口；`package.json` 解析失败时（malformed JSON）不抛错，仅退回到基于文件名的识别。

