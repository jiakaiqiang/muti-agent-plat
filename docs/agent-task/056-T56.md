# T56 Server-local readFile

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T55

## 目标

实现安全文本读取。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 tmpdir 单测。

## 验收标准

- 敏感、二进制和超大文件受控。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-read-file.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-read-file.ts`，`readServerLocalFile` 返回 `ReadFileResult`。
  - 三重受控：`isSensitivePath` 命中即拒绝、含 `\0` 字节的二进制内容拒绝、`maxBytes` 超出时截断并 `truncated=true`；默认 `maxBytes=512KB`，二进制探测前 8000 字节。
  - 支持 `startLine`/`endLine` 行切片；`hash` 始终按整文件计算，避免切片后 revision/hash 不一致。

