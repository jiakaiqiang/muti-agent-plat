# T08 前端扫描忽略生成目录

- 状态：completed
- 预计时间：10 分钟
- 阶段：B
- 依赖：T07

## 目标

让 browser_local 扫描器使用统一忽略清单。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 local scanner 测试。

## 验收标准

- .nuxt 不进入正文扫描。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。browser_local 扫描器通过 T07 的共享生成目录忽略规则跳过 `.nuxt`，不会读取 `.nuxt` 下生成文件正文。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\stores\local-workspace-scanner.coverage.spec.ts`（在 `apps/web` 下执行）；`npm run build -w @project/web`
- 备注：本任务行为由 T07 的统一规则提供，本任务补充了 browser_local 针对 `.nuxt` 的专门回归测试。
