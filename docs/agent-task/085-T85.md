# T85 Agent Profile 跨 Runtime 保持

- 状态：pending
- 预计时间：10 分钟
- 阶段：K
- 依赖：T84

## 目标

确保切换 Runtime 不改变 Agent 身份。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 Orchestrator 单测。

## 验收标准

- role、skills、capabilities 保持一致。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/pair-agent-with-execution-target.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/pair-agent-with-execution-target.ts`，`pairAgentWithExecutionTarget(agent, target)` 返回一个只切换 `runtimeType`（和可选 `modelId`）的新 `RuntimeAgentProfile`。
  - 原对象保持不变（不可变式返回）；`role`、`name`、`systemPrompt`、`capabilityIds`、`skillIds` 与其余身份字段完全沿用旧值，满足“切换 Runtime 不改变 Agent 身份”的验收标准。
  - 当 `target.modelId` 未指定时，保留 agent 原有的 `modelId`，避免路由切换时误清空模型偏好。

