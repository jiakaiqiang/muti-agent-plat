# T22 裁剪 Snapshot 文件元数据

- 状态：completed
- 预计时间：15 分钟
- 阶段：D
- 依赖：T21

## 目标

修复 compactWorkspaceSnapshot 未限制 files 的问题。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 token-fit 测试。

## 验收标准

- 不再保留全部 80 个文件对象。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`compactWorkspaceSnapshot()` 在按相关性排序后，按当前阶段的 `workspaceContentFileCount` 裁剪文件对象；initial 阶段最多保留 8 个，不再携带全部 80 个 Snapshot 元数据。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（在 `apps/server` 下执行，6 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：目录导航仍由裁剪后的 Tree 和 Workspace Manifest 提供；Snapshot 文件对象额度随 focused/compact/minimal 等阶段继续递减。
