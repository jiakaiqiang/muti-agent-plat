# T50 Workspace 冲突错误

- 状态：completed
- 预计时间：10 分钟
- 阶段：G
- 依赖：T49

## 目标

定义 baseHash 不一致的标准错误。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行合同测试。

## 验收标准

- 冲突可结构化返回。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：新增稳定错误码 `WORKSPACE_BASE_HASH_MISMATCH`、`WorkspaceConflictErrorCode` 与结构化 `WorkspaceConflictError`；载荷包含 ChangeSet ID、update/delete/move 操作、冲突路径、期望 baseHash、实际 hash（可缺省）和实际 workspace revision。
- 验证：先新增冲突错误合同测试并运行 shared typecheck，按预期报告错误类型与常量缺失；实现后 `npm run typecheck --workspace=@agent-cluster/shared`、`npx tsx packages/shared/src/workspace-conflict-error.contract.test.ts` 与相关 `git diff --check` 均通过（仅有既有行尾转换警告）。
- 备注：错误码和字段对齐 `system-design.md` 的 baseHash 校验阶段；operation 排除 create，避免把“目标已存在”等其他冲突错误误归类为 baseHash 不一致。
