# T36 Schema Repair 失败错误

- 状态：completed
- 预计时间：10 分钟
- 阶段：E
- 依赖：T35

## 目标

修复失败时返回 OUTPUT_SCHEMA_INVALID。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 Generic LLM 错误测试。

## 验收标准

- 错误包含验证信息且不伪造成功。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。GenericLlmRuntime 不再把无法归一化的 Artifact 静默过滤成合法空数组；任一无效项会保留为 schema 失败并触发一次 repair，修复后仍非法则返回 `status='failed'` 与 `error.code='OUTPUT_SCHEMA_INVALID'`。Tool-loop 最终输出也新增同一严格校验。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\generic-llm-runtime.service.spec.ts .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts .\src\modules\runtimes\runtime-output-schema.spec.ts`（在 `apps/server` 下执行，33 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：错误 details 包含 `parseState='schema_invalid'`、`validationErrors` 与 `repairAttempts`；回归测试验证两次模型响应都含未知 Artifact 类型时不会伪造 completed。
