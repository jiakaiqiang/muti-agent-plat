# T47 Workspace 操作合同

- 状态：completed
- 预计时间：15 分钟
- 阶段：G
- 依赖：T46

## 目标

定义 list、stat、read、search 输入输出。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行合同测试。

## 验收标准

- 四类操作合同完整。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：新增 Workspace Plane 的 `ListDirectoryInput/Result`、`StatFileInput`/`FileMetadata`、`ReadFileInput/Result`、`SearchTextInput/Match/Result` 共享合同；覆盖目录深度与分页、文件元数据、按行/字节读取以及带路径和行列信息的文本搜索。
- 验证：先新增合同测试并运行 shared typecheck，按预期报告九个操作合同导出缺失；实现后 `npm run typecheck --workspace=@agent-cluster/shared`、`npx tsx packages/shared/src/workspace-operations.contract.test.ts` 与相关 `git diff --check` 均通过（仅有既有行尾转换警告）。
- 备注：这些类型独立于现有 LLM `WorkspaceToolsService` 的 `ok/output` 工具协议，避免把 Provider 层与特定 Runtime 返回格式耦合；revision/hash 按任务顺序留给 T48。
