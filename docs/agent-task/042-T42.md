# T42 Session 保留 Ask User 动作

- 状态：completed
- 预计时间：10 分钟
- 阶段：F
- 依赖：T41

## 目标

避免只保留 reason 字符串。

## 实施范围

apps/server/src/modules/orchestrator、sessions；前端 ConfirmationCard

## 测试

运行 SessionsService 单测。

## 验收标准

- Confirmation payload 保留 actions。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：`SessionsService.applyOutcome` 现将 `ask_user` 的结构化 `actions` 同时保留在状态事件与 `user_confirmation_requested` payload；前端 `ConfirmationRequestedPayload`、`ConfirmationCardState`、Event Store 和 ChatTimeline 归并路径均继续携带 actions，不再只剩 reason 字符串。
- 验证：新增 SessionsService 测试修改前按预期失败（4 项中 3 通过、1 失败，payload.actions 为 undefined）；实现后 `npx tsx --test src/modules/sessions/sessions.service.spec.ts` 4/4 通过，server typecheck 与 `npm exec --workspace=@project/web -- vue-tsc --noEmit` 通过，相关 `git diff --check` 通过（仅有既有行尾转换警告）。
- 备注：曾尝试不存在的 `@agent-cluster/web` workspace/typecheck 脚本并得到命令错误，随后按实际包名 `@project/web` 使用 `vue-tsc --noEmit` 完成有效验证；动作视觉渲染留给 T43。
