# T74 Evidence 去重

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T73

## 目标

避免同一路径和同一 Artifact 重复注入。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 selector 单测。

## 验收标准

- 重复 Evidence 只保留一项。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/dedupe-evidence.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/dedupe-evidence.ts`，`dedupeEvidence(files)` 按 (path) 与 (hash.algorithm+value) 两层去重，保留首个出现的实例，返回稳定顺序。
  - 场景覆盖：同路径重复、内容相同但路径不同（hash 相同）、无 hash 时按路径去重、后来出现的副本不会顶掉前一份。

