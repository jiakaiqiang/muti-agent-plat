# T23 Evidence Floor 测试

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T22

## 目标

为 Grounded Task 添加最小证据测试。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 token-fit 测试。

## 验收标准

- 需要源码判断时至少保留一条正文。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 Grounded Source Analysis 的 Evidence Floor 失败测试：`mixed/analysis` 任务带唯一 `src/main.ts` 正文，在预算压力下仍应至少保留一条 `selectedEvidenceContents`。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（在 `apps/server` 下执行）；修改前 7 项中 6 项通过、1 项按预期失败，finalStage 为 `minimal` 且 Evidence 被清空。
- 备注：失败并非 navigation_only 的最终降级造成，而是 minimal 阶段将 `selectedEvidenceItemLimit` 设为 0；修复严格留给依赖任务 T24。
