# T02 Session 记录 Pipeline 版本

- 状态：completed
- 预计时间：10 分钟
- 阶段：A
- 依赖：T01

## 目标

在 SessionDetail 中增加 contextPipelineVersion。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/sessions、ops；apps/web 版本展示

## 测试

运行 Session 合同测试。

## 验收标准

- 字段兼容旧数据，新 Session 可写入版本。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`SessionDetail` 新增可选 `contextPipelineVersion` 以兼容旧数据；新建 Session 会写入默认 `v1`；web 设置页显示当前会话 Pipeline 版本并对旧会话回退默认值。
- 验证：`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`；`npm run build -w @project/web`；`npm run test -w @agent-cluster/server`
- 备注：直接使用根目录 `tsx --test` 单跑服务测试时未加载 Nest 装饰器配置，已改用 server 包测试脚本验证通过。
