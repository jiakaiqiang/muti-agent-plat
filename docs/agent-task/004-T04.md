# T04 新会话固定 v1 或 v2

- 状态：completed
- 预计时间：15 分钟
- 阶段：A
- 依赖：T03

## 目标

根据 Feature Flag 为新会话固定 Pipeline 版本。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/sessions、ops；apps/web 版本展示

## 测试

运行 SessionsService 单测。

## 验收标准

- 新 Session 正确记录版本且运行中不变化。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `contextPipelineVersionForNewSession()`，新 Session 创建时按 `CONTEXT_PIPELINE_V2_ENABLED` 固定为 `v1` 或 `v2`；已创建 Session 的版本字段不会随 flag 变化而变化；ops health 展示默认版本、当前新会话版本和 flag 状态。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\sessions\sessions.service.spec.ts`（在 `apps/server` 下执行）；`npm run typecheck -w @agent-cluster/server`；`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\common\runtime-config.spec.ts`（在 `apps/server` 下执行）
- 备注：仅固定 Session 版本，尚未切换 Context 构建实现；Context v2 具体管线在后续 T69+ 任务中实现。
