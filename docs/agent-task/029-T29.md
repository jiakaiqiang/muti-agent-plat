# T29 GLM Token 诊断事件

- 状态：completed
- 预计时间：15 分钟
- 阶段：D
- 依赖：T28

## 目标

估算误差过阈值时生成诊断事件。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 Generic LLM 单测。

## 验收标准

- 事件包含模型、估算值和实际值。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `inputTokenEstimationDrift()`，当 GLM 实际输入 token 为正且 `abs(actual / estimated - 1) > 0.2` 时，GenericLlmRuntime 追加 `TOKEN_ESTIMATION_DRIFT` 诊断事件；非流式结果由 Orchestrator 写入会话时间线。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\generic-llm-runtime.service.spec.ts .\src\modules\runtimes\runtime.service.spec.ts .\src\modules\orchestrator\orchestrator.service.spec.ts .\src\common\token-fit.spec.ts`（在 `apps/server` 下执行，47 项通过）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：事件为 `runtime_progress`，metadata 包含 `code`、`model`、`estimated`、`actual`、`ratio`、`relativeError` 和 `threshold`；实际 usage 为 `0` 时视为供应商未提供数据，不误报漂移。
