# T90 Browser 命令任务阻塞

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T89

## 目标

命令任务无 server_local/bridge 时明确阻塞。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 resolver 单测。

## 验收标准

- 提示连接 server_local 或 local_bridge。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/evaluate-command-availability.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/evaluate-command-availability.ts`，`evaluateCommandAvailability(input)` 返回 `{ok:true}` 或 `{ok:false, reason, hint}`。
  - 触发阻塞：`requiredCapabilities` 含 `command`，或 `writeMode='direct_audited'`；同时 workspace provider 不属于 `server_local`/`local_bridge`，或 workspace 没有暴露 `command` capability。
  - 阻塞 hint 明确提示“Connect a server_local workspace or local_bridge to execute commands”，满足验收标准；其它情况直接放行。

