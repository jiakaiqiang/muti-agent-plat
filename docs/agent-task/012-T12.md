# T12 服务端优先级扫描

- 状态：completed
- 预计时间：15 分钟
- 阶段：B
- 依赖：T11

## 目标

服务端按相同优先级读取正文。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 tmpdir 扫描测试。

## 验收标准

- 输出优先顺序与前端一致。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。server_local 扫描器与 browser_local 一致，先枚举可读候选，再按 `workspaceFileScanPriority()` 和路径稳定排序读取正文；`workspaceSnapshot.files` 的输出顺序也保持该优先级。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\workspace-scanner.priority.spec.ts .\\src\\common\\workspace-scanner.coverage.spec.ts`（在 `apps/server` 下执行，6 项通过）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\stores\\local-workspace-scanner.priority.spec.ts`（在 `apps/web` 下执行，3 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：tmpdir 回归用例在 80 个普通 Markdown 后创建 `src/main.ts`，确认服务端仍读取入口，且输出顺序与前端使用的纯函数一致。
