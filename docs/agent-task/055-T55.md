# T55 Server-local statFile

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T54

## 目标

实现文件元数据和 hash 查询。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 tmpdir 单测。

## 验收标准

- 返回 size、hash、mtime。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-stat-file.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-stat-file.ts`，`statServerLocalFile({rootPath, revision, input})` 返回 `FileMetadata`。
  - 文件：`kind:'file'` + `size` + `sha256 hash` + `modifiedAt` + `revision`；目录：`kind:'directory'` + `modifiedAt` + `revision`。
  - 路径由 T53 `resolveWorkspacePath` 校验，越界抛错，ENOENT 由 `fs.stat` 直接抛出。

