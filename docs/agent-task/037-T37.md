# T37 真实 GLM 异常 Fixture

- 状态：completed
- 预计时间：15 分钟
- 阶段：E
- 依赖：T36

## 目标

把本次异常输出形状固化为回归 Fixture。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 Fixture 回归测试。

## 验收标准

- 报告正文和类型被正确修复。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增原始 OpenAI-compatible GLM 响应 fixture `fixtures/glm-architecture-artifact-anomaly.json`，固化嵌套 JSON 字符串中的 `architecture_analysis`、缺顶层正文和 `metadata.content` 异常形状；Generic LLM 回归测试从文件加载该响应并走完整解析/归一化链。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\generic-llm-runtime.service.spec.ts .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts .\src\modules\runtimes\runtime-output-schema.spec.ts`（在 `apps/server` 下执行，34 项通过）；`npm run typecheck -w @agent-cluster/server`；PowerShell `Get-Content -Encoding utf8 | ConvertFrom-Json` fixture 解析通过。
- 备注：断言最终 Artifact 为标准 `markdown`，标题和报告正文保留，`reportKind` 保留，且 metadata 中不再存在 `content` 键；fixture 不含凭据或敏感原始模型数据。
