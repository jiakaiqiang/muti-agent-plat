# T62 从 Snapshot 构建 Index

- 状态：pending
- 预计时间：15 分钟
- 阶段：I
- 依赖：T61

## 目标

实现 Snapshot 到 WorkspaceIndex 的转换。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 WorkspaceIndexService 单测。

## 验收标准

- 路径、kind、language 正确。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/build-index-from-snapshot.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/build-index-from-snapshot.ts`，`buildIndexFromSnapshot(snapshot, revision)` 输出 `WorkspaceIndexEntry[]`。
  - 通过遍历 `tree` + `files` 合并去重生成条目；目录进入 `kind:'directory'`，文件生成 `kind:'file'` 并包含 `size`、`language`、`hash`（有 `content` 时对 content 做 sha256，否则用 `sha256('')` 兜底）。
  - `generated` 通过 `isGeneratedWorkspaceDirectory` 判断路径任一段是否属于生成目录；`sensitive` 复用 `path-safety.ts#isSensitivePath`。

