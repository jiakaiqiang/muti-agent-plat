# T63 Index 标记 Generated

- 状态：pending
- 预计时间：10 分钟
- 阶段：I
- 依赖：T62

## 目标

识别生成目录和生成文件。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 Index 单测。

## 验收标准

- .nuxt 等条目被正确标记。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/is-generated-workspace-path.spec.ts apps/server/src/modules/workspaces/workspace-index/build-index-from-snapshot.spec.ts` 6/6 通过（含 T62 回归）
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/is-generated-workspace-path.ts`，识别两类生成条目：路径任一段命中 `GENERATED_WORKSPACE_DIRECTORIES`（含 `.nuxt`/`.next`/`dist`/`build`/`coverage` 等），或文件后缀命中 `.d.ts`/`.js.map`/`.min.js`/`.min.css`。
  - `buildIndexFromSnapshot` 已切换到 `isGeneratedWorkspacePath`，确保通过 Snapshot 构建的 Index 与 `.nuxt/router.ts`、`src/util.d.ts` 等条目都被打上 `generated:true`。

