# T26 完整消息 Token 估算

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T25

## 目标

估算 system prompt、schema、example 和 Context。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 token estimator 单测。

## 验收标准

- 额外 Prompt 文本被计入输入估算。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `estimateRuntimeInputTokens()` 分项估算 Context、Agent system prompt、正式输出 Schema、最小 Example 和额外 Runtime 协议文本；Orchestrator 先从输入预算扣除固定 Prompt 开销，再裁剪 Context，并以完整消息总量做最终门禁与诊断。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（在 `apps/server` 下执行，8 项通过）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\orchestrator\\orchestrator.service.spec.ts`（9 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：`TOKEN_CONTEXT_TRIMMED` 和预算超限诊断现在同时记录 total、Context、固定 Prompt 及完整 `inputEstimate` 分项，避免 Schema/Example/System Prompt 成为未计入开销。
