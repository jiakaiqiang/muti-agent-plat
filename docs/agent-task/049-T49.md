# T49 WorkspaceChangeSet 合同

- 状态：completed
- 预计时间：15 分钟
- 阶段：G
- 依赖：T48

## 目标

定义 create、update、delete、move。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行合同测试。

## 验收标准

- 文件变更可完整表达。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：新增 `WORKSPACE_CHANGE_OPERATIONS`、`WorkspaceChangeOperation`、`WorkspaceChange` 判别联合和 `WorkspaceChangeSet` 批次合同，完整表达 create、update、delete、move；批次携带 `baseRevision`，更新、删除、移动携带 `expectedHash`，创建/更新携带 UTF-8 内容。
- 验证：先新增覆盖四个分支的合同测试并运行 shared typecheck，按预期报告四个 ChangeSet 合同导出缺失；实现后 `npm run typecheck --workspace=@agent-cluster/shared`、`npx tsx packages/shared/src/workspace-change-set.contract.test.ts` 与相关 `git diff --check` 均通过（仅有既有行尾转换警告）。
- 备注：使用判别联合而非可选字段集合，类型系统可阻止诸如 delete 携带 content、create 缺少 content 等无效变更组合。
