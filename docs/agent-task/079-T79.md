# T79 Grounded Evidence Gate

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T78

## 目标

在执行前检查任务是否具备必要证据。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 gate 单测。

## 验收标准

- 证据为空时返回 blocked。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/grounded-evidence-gate.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/grounded-evidence-gate.ts`，`evaluateGroundedEvidenceGate({envelope, requiresEvidence})` 返回 `{ok:true}` 或 `{ok:false, reason}`。
  - 三种阻断原因：`evidence-empty`（要求证据但 L3.files 为空）、`evidence-only-generated`（全部证据条目在 Manifest 中标记 generated）、`evidence-only-sensitive`（全部证据条目为敏感文件）。
  - `requiresEvidence=false` 时直接放行；证据在 Manifest 中找不到条目（例如 supplemental 补读）也视为可用，不误报。

