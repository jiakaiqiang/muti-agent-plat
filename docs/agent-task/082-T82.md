# T82 架构分析路由测试

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T81

## 目标

定义架构任务可使用的 Runtime 集合。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 resolver 单测。

## 验收标准

- Generic、CodeReader、Codex、Claude 可按能力选择。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/resolve-architecture-runtimes.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/resolve-architecture-runtimes.ts`，`resolveArchitectureRuntimes(input)` 返回按能力过滤后的架构分析 Runtime 列表。
  - 架构候选集：`generic_llm`、`code_reader`、`codex`、`claude_code`；根据 `requiredCapabilities` 与 workspace `capabilities` 两侧同时过滤，任意一侧不满足即排除。
  - 例：只需 `read` 时四者均可；追加 `write` 时只保留 codex/claude_code；workspace 不给 write 时返回空列表。

