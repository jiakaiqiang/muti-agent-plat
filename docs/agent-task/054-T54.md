# T54 Server-local listDirectory

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T53

## 目标

实现服务端目录列表操作。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 tmpdir 单测。

## 验收标准

- 生成目录被忽略且结果受限。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run build --workspace=@agent-cluster/shared` 通过（把 T45–T51 的合同产出物同步到 dist，供 tsx --test 运行时使用）
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-list-directory.spec.ts` 4/4 通过
  - workspaces 已存在的 4 个 spec（provider contract / registry / path / list-directory）合计 16/16 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-list-directory.ts`，`listServerLocalDirectory({rootPath, revision, input})` 返回 `ListDirectoryResult`。
  - 使用 T53 的 `resolveWorkspacePath` 校验入参路径；生成目录（`GENERATED_WORKSPACE_DIRECTORIES`）在结果中被剔除；文件结果通过 `sha256` 得到 `FileHash`，与 `WorkspaceRevision` 一起返回。
  - `limit` + `cursor`（base64url 编码的偏移）实现分页与 `nextCursor` 截断，符合“结果受限”的验收标准。

