# T38 证据不足状态失败测试

- 状态：completed
- 预计时间：10 分钟
- 阶段：F
- 依赖：T37

## 目标

添加证据不足不得 task_completed 的测试。

## 实施范围

apps/server/src/modules/orchestrator、sessions；前端 ConfirmationCard

## 测试

运行 Orchestrator 单测并确认修改前失败。

## 验收标准

- 测试覆盖 blocked 语义。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已在 Orchestrator 主链新增“证据不足的 blocked 执行必须等待且不得发出 task_completed”回归测试，覆盖任务状态、task_waiting 事件和 task_completed 禁止条件。
- 验证：`npx tsx --test src/modules/orchestrator/orchestrator.service.spec.ts`（修改生产逻辑前按预期失败：11 项中 10 通过、1 失败；新增用例实际得到 `failed`，期望 `waiting`）。
- 备注：该红灯基线证明 blocked 当前被错误归类为普通模型失败；依赖任务 T39 负责修复分流并恢复绿灯。
