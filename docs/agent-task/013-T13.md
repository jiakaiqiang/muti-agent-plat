# T13 分离 Tree 与正文限额

- 状态：completed
- 预计时间：10 分钟
- 阶段：B
- 依赖：T12

## 目标

正文额度耗尽后仍继续构建目录树。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 scanner limit 测试。

## 验收标准

- 正文满额不影响完整导航统计。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增前后端正文限额回归测试，证明 81 个可读文件全部保留在 Tree 和导航统计中，而正文严格限制为 80 个并审计 1 个 `limit_exceeded`。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\stores\\local-workspace-scanner.coverage.spec.ts`（在 `apps/web` 下执行，3 项通过）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\workspace-scanner.coverage.spec.ts`（在 `apps/server` 下执行，4 项通过）
- 备注：本任务新增测试时已因 T11/T12 必需的“先枚举、后读正文”重构而通过，因此未重复修改实现；独立测试固定了 Tree 与正文额度分离的不变量。
