# T03 Context v2 Feature Flag

- 状态：completed
- 预计时间：10 分钟
- 阶段：A
- 依赖：T02

## 目标

增加 v2 Feature Flag 配置读取函数。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/sessions、ops；apps/web 版本展示

## 测试

运行 runtime-config.spec.ts。

## 验收标准

- true、false 和非法值均有确定行为。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `contextPipelineV2Enabled()`，读取 `CONTEXT_PIPELINE_V2_ENABLED`，默认关闭；`1/true/yes/on` 启用，`false/非法/空白` 关闭；ops health 暴露当前 flag 状态。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\common\runtime-config.spec.ts`（在 `apps/server` 下执行）；`npm run typecheck -w @agent-cluster/server`
- 备注：T04 才根据该 Feature Flag 为新会话固定 `v1` 或 `v2`，本任务未改变创建会话版本选择逻辑。
