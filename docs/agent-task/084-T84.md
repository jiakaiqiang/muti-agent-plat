# T84 ExecutionTarget Resolver

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T83

## 目标

实现已确认的路由优先级。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 resolver 单测。

## 验收标准

- 结果包含 runtime、source 和 writeMode。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run build --workspace=@agent-cluster/shared` 通过
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/resolve-execution-target.spec.ts` 6/6 通过
- 备注：
  - 在 shared 增加 `ExecutionTargetSource` 与 `ResolvedExecutionTarget`（注意与既有 `ExecutionTarget` 区分，后者仅含 runtimeType/modelId，本任务不动它以避免破坏旧 spec）。
  - 新增 `apps/server/src/modules/runtime-routing/resolve-execution-target.ts`：优先级 `task_override` > `session_preference`（agent 偏好）> `project_policy` > `smart_router`（外部或候选首个）> `global_default`（`generic_llm`）。
  - 无候选时也能返回 `global_default`；始终把 `requiredCapabilities` 与 `writeMode` 从输入透传到结果。

