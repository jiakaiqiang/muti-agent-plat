# T77 ContextPackBuilderV2

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T76

## 目标

组装 Identity、Navigation、Evidence 和 Policy。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 builder 单测。

## 验收标准

- ContextEnvelopeV2 字段完整。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/context-pack-builder-v2.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/context-pack-builder-v2.ts`，`buildContextEnvelopeV2({phase, sessionId, l0..l6, budget, createdAt})` 输出完整 `ContextEnvelopeV2`（`version:'v2'` 固定）。
  - 每层依据 T70 的 `allowedLayersForPhase(phase)` 决定是否透传原始内容：不允许的层用空占位（保持类型完整）。
  - 用例覆盖 execution（全层携带）、discussion（清空 L3/L4/L6）、post_review（保留 L6 交付物）三档策略。

