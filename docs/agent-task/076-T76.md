# T76 Supplemental Evidence 优先

- 状态：pending
- 预计时间：10 分钟
- 阶段：J
- 依赖：T75

## 目标

让 requestedPaths 获得最高选择优先级。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 selector 单测。

## 验收标准

- 补读文件进入 Selected Evidence。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/prioritize-requested-evidence.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/prioritize-requested-evidence.ts`，`prioritizeRequestedEvidence(candidates, requestedPaths)` 为 requested 路径追加 `+10000` 分并附 `user-requested` 原因；缺失的候选会被凭空注入。
  - 与 T75 联动：只要预算能容纳补读文件，一定被 `selectEvidenceWithinBudget` 选中，验证脚本用一个 requested path 挤过更高分候选来断言。

