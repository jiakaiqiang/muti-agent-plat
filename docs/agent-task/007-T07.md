# T07 统一生成目录忽略清单

- 状态：completed
- 预计时间：10 分钟
- 阶段：B
- 依赖：T06

## 目标

抽取前后端可复用的生成目录忽略规则。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 ignore-rule node:test。

## 验收标准

- 能够识别 .nuxt、.output、.vite、.turbo。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增共享 `GENERATED_WORKSPACE_DIRECTORIES` 与 `isGeneratedWorkspaceDirectory()`，统一覆盖 `.nuxt`、`.output`、`.vite`、`.turbo` 等生成目录；server 与 browser_local 扫描器均改用共享规则。
- 验证：`node .\node_modules\tsx\dist\cli.mjs --test .\packages\shared\src\workspace-ignore.test.ts`；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`；`npm run build -w @project/web`
- 备注：为实现前后端复用，新增规则放在 `packages/shared/src/workspace-ignore.ts`，再由两个扫描器引用。
