# T32 Artifact 归一化函数

- 状态：completed
- 预计时间：15 分钟
- 阶段：E
- 依赖：T31

## 目标

实现旧输出到标准 Artifact 的纯函数转换。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 normalizer 单测。

## 验收标准

- metadata.content 被修复到顶层 content。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增纯函数 `normalizeRuntimeArtifact()`，将旧产物的 `metadata.content` 提升为顶层 `content`，删除 metadata 中的旧正文键并保留其余 metadata；若顶层正文已存在则以顶层为准，且不会修改输入对象。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts`（在 `apps/server` 下执行，3 项通过）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：无可用 `type`、`title` 或正文时返回 `undefined`；本任务仅实现纯函数，Generic LLM 与 streaming 的接入分别留给 T34、T35，未知类型策略留给 T33。
