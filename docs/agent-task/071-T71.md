# T71 Navigation Builder

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T70

## 目标

从 Index 生成受限导航包。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 builder 单测。

## 验收标准

- 只输出有限目录、入口和 coverage。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/build-navigation-manifest.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/build-navigation-manifest.ts`，`buildNavigationManifest({entries, entrypoints, maxEntries})` 返回 `ContextL1NavigationManifest`。
  - 过滤 `generated:true` 与 `sensitive:true` 条目；剩余按“入口 > 目录 > 深度浅”打分排序（入口 +1000、目录 +100、depth 递减）；`maxEntries` 截断并给出 base64url `nextCursor`。
  - 保留输入的最少必要字段（path/kind/generated/sensitive；文件带 size/language），既满足 Context L1 合同又限制体积。

