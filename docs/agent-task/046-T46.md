# T46 WorkspaceCapabilities 合同

- 状态：completed
- 预计时间：15 分钟
- 阶段：G
- 依赖：T45

## 目标

定义读取、写入、命令和测试能力。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行合同测试。

## 验收标准

- 能力可以被 Runtime Router 判断。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：新增 `WORKSPACE_CAPABILITY_KEYS`、`WorkspaceCapabilityKey` 与 `WorkspaceCapabilities` 共享合同，固定 `read`、`write`、`command`、`test` 四项能力，并要求 Provider 以完整只读布尔映射显式声明，Runtime Router 可直接按 capability key 判定。
- 验证：先新增合同测试并运行 shared typecheck，按预期因三个能力合同导出不存在而失败；实现后 `npm run typecheck --workspace=@agent-cluster/shared`、`npx tsx packages/shared/src/workspace-capabilities.contract.test.ts` 与相关 `git diff --check` 均通过（仅有既有行尾转换警告）。
- 备注：遵循 `system-design.md` 中 `WorkspaceProvider.capabilities()` 的边界；本任务只定义能力数据形状，具体 Provider 声明和 Runtime Router 接线留给后续任务。
