# T41 Post Review 缺证据动作

- 状态：completed
- 预计时间：15 分钟
- 阶段：F
- 依赖：T40

## 目标

证据不足时输出 request_workspace_context。

## 实施范围

apps/server/src/modules/orchestrator、sessions；前端 ConfirmationCard

## 测试

运行 Runtime 和 Orchestrator 单测。

## 验收标准

- 动作和缺失路径可追溯。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：Post Review 缺少工作区证据时可生成 `request_workspace_context`，并以非空 `missingPaths` 记录精确相对路径；严格 Schema、Generic LLM、Codex/Claude 流式映射、Mock Runtime、Orchestrator `ask_user` outcome 和复盘事件/制品均保留该动作。Generic、Codex、Claude 的 Post Review 提示也明确禁止在缺证据时宣称复核完成。
- 验证：新增测试修改前分别确认 Schema 拒绝 actions、Generic/流式映射丢 actions、Orchestrator outcome 丢 actions、Runtime 提示与 Mock 不生成动作；实现后 `npx tsx --test src/modules/runtimes/post-review-context-action.spec.ts` 2/2 通过，Runtime/Orchestrator 组合测试 62/62 通过，`npm run typecheck --workspace @agent-cluster/server` 与 shared 类型检查通过，相关 `git diff --check` 通过（仅有既有行尾转换警告）。
- 备注：本任务把动作带到 `ExecutionOutcome`；Session 确认 payload 的保留由依赖任务 T42 实现。
