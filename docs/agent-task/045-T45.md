# T45 WorkspaceProviderKind 合同

- 状态：completed
- 预计时间：10 分钟
- 阶段：G
- 依赖：T44

## 目标

定义 server_local、browser_broker、local_bridge。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行 shared typecheck。

## 验收标准

- 三种 Provider 类型存在。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：在共享合同中新增稳定的 `WORKSPACE_PROVIDER_KINDS` 常量，并由其推导 `WorkspaceProviderKind` 联合类型，包含 `server_local`、`browser_broker`、`local_bridge` 三种 Provider。
- 验证：先新增合同测试并运行 shared typecheck，按预期因缺少 `WorkspaceProviderKind` 与 `WORKSPACE_PROVIDER_KINDS` 导出而失败；实现后 `npm run typecheck --workspace=@agent-cluster/shared`、`npx tsx packages/shared/src/workspace-provider.contract.test.ts` 与相关 `git diff --check` 均通过（仅有既有行尾转换警告）。
- 备注：本任务只定义 Provider 类型合同；现有 `SessionWorkingDirectory.kind` 的迁移和服务端 Provider 实现留给后续工作区任务，避免提前扩大 T45 范围。
