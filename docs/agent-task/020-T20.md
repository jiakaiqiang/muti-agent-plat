# T20 缺少架构师的等待语义

- 状态：completed
- 预计时间：10 分钟
- 阶段：C
- 依赖：T19

## 目标

没有架构师时明确等待而非错误改派。

## 实施范围

apps/server/src/modules/intent-recognition；apps/server/src/modules/orchestrator

## 测试

运行 Orchestrator 单测。

## 验收标准

- 任务不会改派给需求分析师。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 Orchestrator 门禁回归测试：架构分析任务缺少 assignee/architect 时进入 `waiting` 并发出 `task_waiting`，在 Runtime 和 claim 之前返回，不会改派给 requirements。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\orchestrator\\orchestrator.service.spec.ts`（在 `apps/server` 下执行，8 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：测试同时断言 Runtime 调用次数为 0、没有 `task_reassigned`、没有发给 requirements 的事件；现有生产门禁已满足要求，本任务补齐最终行为证据。
