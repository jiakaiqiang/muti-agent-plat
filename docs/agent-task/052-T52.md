# T52 Workspace Provider Registry

- 状态：pending
- 预计时间：10 分钟
- 阶段：G
- 依赖：T51

## 目标

创建按 kind 获取 Provider 的 Registry。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/workspaces

## 测试

运行 Registry 单测。

## 验收标准

- 已注册 Provider 可正确解析。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-provider-registry.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-provider-registry.ts`，`WorkspaceProviderRegistry` 提供 `register`/`resolve`/`has` 三个方法，按 `WorkspaceProviderKind` 存储 Provider。
  - 重复注册与解析未注册 kind 均抛出可诊断的错误信息，避免 Provider 被静默替换或空引用。
  - 未修改 `packages/shared/src/contracts.ts`；本任务的 Registry 仅在 server 侧扩展，接口和类型继续沿用 T51 定义。

