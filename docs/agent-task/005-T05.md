# T05 Health 暴露构建版本

- 状态：completed
- 预计时间：10 分钟
- 阶段：A
- 依赖：T04

## 目标

让 Ops health 返回 build、commit 和 Pipeline 版本。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/sessions、ops；apps/web 版本展示

## 测试

运行 Ops controller 单测。

## 验收标准

- API 返回 buildTime、commit、pipelineVersion。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `OpsHealth` 共享合同；Ops health 返回 `buildTime`、`commit`、`pipelineVersion`，并保留默认/当前 Context Pipeline 版本与 v2 flag 状态。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\ops\ops.controller.spec.ts`（在 `apps/server` 下执行）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：`buildTime` 读取 `AGENT_CLUSTER_BUILD_TIME`/`BUILD_TIME`，`commit` 读取 `AGENT_CLUSTER_COMMIT`/`GIT_COMMIT`/`COMMIT_SHA`，未配置时返回 `unknown`。
