# T10 文件扫描优先级函数

- 状态：completed
- 预计时间：15 分钟
- 阶段：B
- 依赖：T09

## 目标

实现入口、规则、配置、源码优先级纯函数。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 priority 单测。

## 验收标准

- AGENTS、入口和配置高于普通文档。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。前后端扫描器新增同名纯函数 `workspaceFileScanPriority()`，按规则文件、入口、配置、源码、普通文档和其他文件返回稳定的递增优先级；本任务不提前改变扫描顺序。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\stores\\local-workspace-scanner.priority.spec.ts`（在 `apps/web` 下执行）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\workspace-scanner.priority.spec.ts`（在 `apps/server` 下执行）；`npm run typecheck -w @agent-cluster/server`；`npm run build -w @project/web`
- 备注：两端各 2 项 priority 测试通过；扫描排序接入留给后续依赖任务，避免扩大 T10 范围。
