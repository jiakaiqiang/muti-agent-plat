# T01 Context Pipeline 版本类型

- 状态：completed
- 预计时间：10 分钟
- 阶段：A
- 依赖：无

## 目标

在共享合同中增加 ContextPipelineVersion。

## 实施范围

packages/shared/src/contracts.ts；apps/server/src/modules/sessions、ops；apps/web 版本展示

## 测试

新增类型合同测试并运行 shared typecheck。

## 验收标准

- 支持 v1 和 v2，类型检查通过。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。共享合同新增 `ContextPipelineVersion`、`SUPPORTED_CONTEXT_PIPELINE_VERSIONS` 和 `DEFAULT_CONTEXT_PIPELINE_VERSION`，支持 `v1`/`v2`；ops health 返回默认 Pipeline 版本；web 设置页展示默认 Context Pipeline 版本。
- 验证：`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`；`npm run build -w @project/web`
- 备注：按 T02 边界，尚未在 `SessionDetail` 写入 `contextPipelineVersion`，该字段留给 T02 实现。
