# T60 写入后更新 Revision

- 状态：pending
- 预计时间：10 分钟
- 阶段：H
- 依赖：T59

## 目标

写入完成后重新计算工作区 revision。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 Provider 单测。

## 验收标准

- revision 和文件 hash 更新。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-revision.spec.ts apps/server/src/modules/workspaces/workspace-apply-change-set.spec.ts` 6/6 通过（含 T59 回归）
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-revision.ts`，`createWorkspaceRevision()` 使用 `crypto.randomUUID` 与 `nowIso()` 生成新 `WorkspaceRevision`。
  - `workspace-apply-change-set.ts` 中 `nextRevision` 改为可选，缺省时调用 `createWorkspaceRevision()`，从而“写入完成后自动更新工作区 revision”。
  - `statServerLocalFile` / `readServerLocalFile` 在写入后使用新 revision 采样时，文件 hash 会自然反映最新内容（在 T60 spec 用一个 update+stat 端到端验证）。

