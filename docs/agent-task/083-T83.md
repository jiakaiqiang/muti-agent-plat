# T83 编码任务路由测试

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T82

## 目标

定义命令和写入能力要求。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 resolver 单测。

## 验收标准

- Browser Broker 不会被选为命令执行环境。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/resolve-coding-runtimes.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/resolve-coding-runtimes.ts`，`resolveCodingRuntimes(input)` 返回可承担编码任务的 Runtime。
  - 候选仅限 `codex` 和 `claude_code`；`requiredCapabilities` 为空时默认按编码任务要求 `read+write+command`，因此 command 缺失的 workspace（如 browser_broker 默认能力）会被剔除，符合“Browser Broker 不会被选为命令执行环境”的验收标准。
  - 仅需 `read+write` 时，browser_broker 仍可保留 codex/claude_code（用于 propose_changes 模式的纯写入不带命令）。

