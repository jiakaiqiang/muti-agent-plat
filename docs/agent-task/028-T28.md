# T28 Token 估算误差记录

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T27

## 目标

保存 estimated、actual 和 ratio。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 RuntimeService 单测。

## 验收标准

- Invocation 可查询估算误差。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`AgentRunInput.estimatedInputTokens` 由 Orchestrator 传入完整输入估算；RuntimeService 将其与 `usage.inputTokens` 配对，并把 `{ estimated, actual, ratio }` 保存到 Invocation 的 `inputTokenEstimation` 字段。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\runtime.service.spec.ts .\src\modules\orchestrator\orchestrator.service.spec.ts`（在 `apps/server` 下执行，15 项通过）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：`ratio` 定义为 `actual / estimated`；`listInvocations()` 及 `/api/sessions/:sessionId/debug/runtime-invocations` 可直接查询误差记录，持久化集合 `runtimeInvocationsBySession` 同步保存该字段。
