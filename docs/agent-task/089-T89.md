# T89 Browser 分析任务降级

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T88

## 目标

Browser 无 CLI 时将分析任务降级 Generic。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 resolver 单测。

## 验收标准

- 记录 fallback source 和 reason。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/resolve-with-browser-fallback.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/resolve-with-browser-fallback.ts`，`resolveWithBrowserFallback(args)` 复用 T84 的 resolver，然后判断是否需要降级。
  - 降级条件：`workspace.providerKind === 'browser_broker'` 且 `capabilities.command === false`，且原选中的 Runtime 属于命令重型 (`codex` / `claude_code`)。
  - 降级结果：`runtimeType='generic_llm'`、`source='smart_router'`、附 `fallbackFrom` 与 `fallbackReason='browser-broker-lacks-command-capability'`，便于审计路由决策；其它情况保持原目标。

