# T64 Index 标记 Sensitive

- 状态：pending
- 预计时间：10 分钟
- 阶段：I
- 依赖：T63

## 目标

识别凭据和敏感配置文件。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 Index 单测。

## 验收标准

- .env、.npmrc、密钥文件被标记。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/is-sensitive-workspace-path.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/is-sensitive-workspace-path.ts`，为 workspace-index 复用 `common/path-safety.ts#isSensitivePath` 的分类结果。
  - `.env`（含 `.env.production` 等派生名）、`.npmrc`、`.gitconfig`、`.netrc`、SSH 私钥（`id_rsa`）、`.pem`/`.key`/`.crt` 密钥文件、`api-key`/`secret`/`token` 命名文件均被标记 `sensitive:true`。
  - `buildIndexFromSnapshot` 已改为通过新的 `isSensitiveWorkspacePath` 判断，端到端 spec 断言 `.env` 与 `config/api-key.json` 打上 `sensitive:true`，普通源码保持 `false`。

