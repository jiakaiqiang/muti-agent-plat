# T17 推导 requiresCodeChanges

- 状态：completed
- 预计时间：10 分钟
- 阶段：C
- 依赖：T16

## 目标

为意图结果增加是否需要修改代码的语义。

## 实施范围

apps/server/src/modules/intent-recognition；apps/server/src/modules/orchestrator

## 测试

运行合同和服务单测。

## 验收标准

- 架构分析返回 false。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。IntentRecognition 结果新增 `requiresCodeChanges`；新 Session 固化该语义，Context Router 与 Orchestrator 统一优先读取 Session 字段，并为旧 Session 按 intent 回退。架构分析为 `false`，实现请求为 `true`。
- 验证：`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\intent-recognition\\intent-recognition.service.spec.ts .\\src\\modules\\orchestrator\\context-router.service.spec.ts .\\src\\modules\\sessions\\sessions.service.spec.ts`（在 `apps/server` 下执行，12 项通过）
- 备注：字段在 `SessionDetail` 中保持可选以兼容旧持久化数据；旧数据不再按 mixed/coding domain 粗略判定，而按 `intent === 'implementation'` 回退。
