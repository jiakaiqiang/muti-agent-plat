# T25 Navigation-only 禁止完成分析

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T24

## 目标

将无证据的导航模式转为上下文不足。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 Orchestrator 单测。

## 验收标准

- 分析任务不再在 navigation_only 下完成。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`runRuntime()` 在 adapter 启动前检查裁剪诊断：analysis 若已降级为 `navigation_only` 且没有任何 Evidence 正文，直接返回可重试 `CONTEXT_INSUFFICIENT`，请求原始选中 Evidence、入口和相关路径。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\orchestrator\\orchestrator.service.spec.ts`（在 `apps/server` 下执行，9 项通过）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（7 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：门禁测试使用自校准 navigation_only 预算，断言 Runtime 调用次数为 0、错误码为 `CONTEXT_INSUFFICIENT`、`requestedPaths` 包含 `src/main.ts`。
