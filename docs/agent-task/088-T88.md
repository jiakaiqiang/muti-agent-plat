# T88 Claude 使用精简 Context v2

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T87

## 目标

为 Claude 使用相同分层约束。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 Claude Adapter 单测。

## 验收标准

- Prompt 与 Context v2 合同一致。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/slim-context-for-claude.spec.ts` 2/2 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/slim-context-for-claude.ts`，`slimContextForClaude(envelope, rulesSummary?)` 输出 Claude 使用的精简 Context。
  - 与 Context v2 合同一致：`version:'v2'`、`identity` 直接来自 L0、`navigation` 拷贝 L1 的 path/kind/language、`projectMap` 保留 L2 模块元信息（含 entrypoints/tests）、`evidenceSummary` 只暴露 L3 的 fileCount/totalByteLength/truncated/paths。
  - 与 Codex 精简版差异：Claude 拿到 ProjectMap 与 evidenceSummary（用于自主拉取），但 Evidence 正文仍不外泄，spec 断言 `secret content` 不出现在序列化输出中。

