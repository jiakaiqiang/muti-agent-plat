# T58 ChangeSet baseHash 检查

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T57

## 目标

写入前检查源文件版本。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 tmpdir 冲突测试。

## 验收标准

- 源文件变化时返回 conflict。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-change-set-validation.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-change-set-validation.ts`，`validateChangeSetBaseHashes({rootPath, currentRevision, changeSet})` 返回 `WorkspaceConflictError[]`。
  - `create` 跳过；`update`/`delete` 用 `change.path` 与磁盘 sha256 对比，`move` 用 `change.fromPath`。
  - 目标文件不存在时也产生 `WORKSPACE_BASE_HASH_MISMATCH` 冲突（`actualHash` 缺省，`message` 明确写“no longer exists”）。

