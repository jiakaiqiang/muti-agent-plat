# T81 Runtime Routing Input

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T80

## 目标

定义阶段、任务、Agent、Workspace 能力输入。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 shared typecheck。

## 验收标准

- 路由所需信息完整。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run build --workspace=@agent-cluster/shared` 通过
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
- 备注：
  - 在 `packages/shared/src/contracts.ts` 增加 `RuntimeRoutingInput` 与相关类型：`RuntimeRoutingPhase`、`RuntimeRoutingWriteMode`、`RuntimeRoutingWorkspaceContext`、`RuntimeRoutingOverride`。
  - 输入字段：`phase`、`sessionId`、`taskKind`、`agentId`、可选 `agentPreferredRuntime`、`requiredCapabilities`（引用 `WorkspaceCapabilityKey`）、`writeMode`（none/propose_changes/direct_audited）、`workspace`（workspaceId + providerKind + capabilities）、可选 `userOverride`。
  - 新增 `packages/shared/src/runtime-routing-input.contract.test.ts`，用 `Assert<IsExact>` 固定 phase 与 writeMode 的字符串字面量集合，并示例化一个 execution 阶段的 routing 输入。

