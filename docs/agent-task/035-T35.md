# T35 Streaming 接入归一化

- 状态：completed
- 预计时间：15 分钟
- 阶段：E
- 依赖：T34

## 目标

Codex 和 Claude 流式映射使用相同归一化。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 frame mapper 单测。

## 验收标准

- 不同 Runtime 产物合同一致。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`framesToOutput()` 的 `task_execution_result` 映射现在对 payload 中每个 Artifact 调用 `normalizeRuntimeArtifact()`；Codex 与 Claude streaming runner 均经该共同 mapper 进入严格 schema，因此旧正文迁移、类型映射与未知类型过滤保持一致。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\streaming\frame-to-output.mapper.spec.ts .\src\modules\runtimes\streaming\codex-streaming-runner.spec.ts .\src\modules\runtimes\streaming\claude-streaming-runner.spec.ts .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts .\src\modules\runtimes\runtime-output-schema.spec.ts`（在 `apps/server` 下执行，39 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：payload Artifact 全部无效时仍保留既有 write/edit/create tool frame 推导 `code_diff` 的兜底；推导产物自身符合标准合同。
