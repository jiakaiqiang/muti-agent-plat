# T68 WorkspaceIndex 缓存

- 状态：pending
- 预计时间：15 分钟
- 阶段：I
- 依赖：T67

## 目标

按 workspaceId 和 revision 缓存索引。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 service 单测。

## 验收标准

- 同 revision 命中缓存，新 revision 重建。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/workspace-index-service.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/workspace-index-service.ts`，`WorkspaceIndexService` 用内部 `Map<workspaceId, WorkspaceIndexCache>` 组合，把 T67 的 revision 敏感缓存扩展成多 workspace 隔离。
  - 接口：`setIndex(workspaceId, revision, entries)` / `getIndex(workspaceId, revision)` / `invalidateOn(workspaceId, newRevision)` / `drop(workspaceId)` / `currentRevisionId(workspaceId)`。
  - 同 revision 命中缓存，新 revision `invalidateOn` 清空该工作区索引；不同 workspaceId 完全独立，不会相互覆盖。

