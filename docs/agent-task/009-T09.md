# T09 服务端扫描忽略生成目录

- 状态：completed
- 预计时间：10 分钟
- 阶段：B
- 依赖：T08

## 目标

让 server_local 扫描器使用统一忽略清单。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 server scanner 测试。

## 验收标准

- 前后端忽略结果一致。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。server_local 扫描器使用共享生成目录忽略规则，`.nuxt`、`.output`、`.vite`、`.turbo` 等生成目录不会进入正文读取，并会记录为 `ignored_directory`。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\common\workspace-scanner.coverage.spec.ts`（在 `apps/server` 下执行）；`npm run typecheck -w @agent-cluster/server`
- 备注：前端在 T08 已验证 `.nuxt` 跳过；本任务补充 server_local 对共享清单的回归测试，前后端通过同一 `isGeneratedWorkspaceDirectory()` 保持一致。
