# T61 WorkspaceIndexEntry 合同

- 状态：pending
- 预计时间：10 分钟
- 阶段：I
- 依赖：T60

## 目标

定义完整索引条目类型。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 shared typecheck。

## 验收标准

- 路径、类型、hash、generated、sensitive 字段齐全。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run build --workspace=@agent-cluster/shared` 通过（同步 dist 供后续 server 运行时导入）
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
- 备注：
  - 在 `packages/shared/src/contracts.ts` 增加 `WorkspaceIndexEntryKind`（`'file' | 'directory'`）与 `WorkspaceIndexEntry` 判别联合。
  - 文件条目：`kind:'file'` 必需 `size`/`hash`，可选 `language`/`modifiedAt`；目录条目：`kind:'directory'`，无 `size`/`hash`。
  - 两种条目共享 `path`/`revision`/`generated`/`sensitive` 字段，覆盖“路径、类型、hash、generated、sensitive 字段齐全”的验收标准。
  - 新增 `packages/shared/src/workspace-index-entry.contract.test.ts`，用 `satisfies` 与判别联合断言四类样本（file/dir/generated/sensitive）都能通过类型检查。

