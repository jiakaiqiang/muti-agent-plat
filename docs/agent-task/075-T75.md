# T75 Evidence 预算选择

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T74

## 目标

在预算内选择最高价值正文。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 selector 单测。

## 验收标准

- 输出不超预算且保留高分证据。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/select-evidence-within-budget.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/select-evidence-within-budget.ts`，`selectEvidenceWithinBudget({candidates, contents, budgetBytes})` 返回 `ContextL3SelectedEvidence`。
  - 贪心策略：按分数降序遍历候选，能装入的即收纳，装不下的跳过继续尝试更小候选；`totalByteLength` 累积、`truncated` 标记有候选被丢弃或未被完全采纳。
  - 同分候选按路径字典序稳定排序，避免结果非确定。

