# T18 分析任务默认不写文件

- 状态：completed
- 预计时间：10 分钟
- 阶段：C
- 依赖：T17

## 目标

防止 analysis 自动产生文件写入任务。

## 实施范围

apps/server/src/modules/intent-recognition；apps/server/src/modules/orchestrator

## 测试

运行 Orchestrator 单测。

## 验收标准

- 未明确要求时不产生 fileChanges。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 Orchestrator 入库边界回归测试：即使 Runtime 错误返回 `fileChanges`，未明确要求写文件的 analysis 会话也会在执行 artifact 顶层和嵌套 runtime artifact 中清空；显式写入请求仍保留变更。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\orchestrator\\orchestrator.service.spec.ts`（在 `apps/server` 下执行，7 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：现有 Orchestrator 已在 `createExecutionArtifact()` 入库边界实施强制清空，本任务补充最终可观察 artifact 的回归证据，而非仅依赖 Runtime 提示词。
