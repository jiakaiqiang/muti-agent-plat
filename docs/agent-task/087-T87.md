# T87 Codex 使用精简 Context v2

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T86

## 目标

为 Codex 只注入任务、规则和导航。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 Codex Adapter 单测。

## 验收标准

- Prompt 不含全量 Snapshot。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/slim-context-for-codex.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/slim-context-for-codex.ts`，`slimContextForCodex(envelope, rulesSummary?)` 输出 Codex 使用的最小 Context 结构。
  - 保留：`workspaceId`/`sessionId`/`identity`(L0)、`navigation`（仅 L1 路径与 kind）、`navigationTruncated`、`rulesSummary`（外部传入的规则/系统 Prompt 摘要）。
  - 剔除：L3 Evidence 正文、L2 ProjectMap 模块细节、L4/L5/L6 全部；spec 断言序列化结果不包含 Evidence 中的 `const secret = 1;` 与 `massiveSnapshot` 关键词，满足“Prompt 不含全量 Snapshot”的验收标准。

