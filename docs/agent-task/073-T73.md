# T73 Evidence 候选评分

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T72

## 目标

实现用户文件、入口、合同优先评分。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 selector 单测。

## 验收标准

- 高价值文件排名靠前。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/score-evidence-candidates.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/score-evidence-candidates.ts`，`scoreEvidenceCandidates({entries, entrypoints, userMentionedPaths, userKeywords})` 返回 `ScoredEvidenceCandidate[]`（含 path/score/reasons，按 score 降序）。
  - 权重：user-mentioned +500、entrypoint +200、contract-like +120、test +40、keyword-match 每命中 +30、深度浅额外加分。
  - 生成/敏感文件直接剔除；`reasons` 数组便于后续 UI 展示为什么被选入 Evidence。

