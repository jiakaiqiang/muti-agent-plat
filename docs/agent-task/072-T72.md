# T72 ProjectMap v2 适配器

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T71

## 目标

从 WorkspaceIndex 构建 ProjectMap。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 ProjectMap 单测。

## 验收标准

- 模块、入口、合同和测试可追溯。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/build-project-map.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/build-project-map.ts`，`buildProjectMap({entries, entrypoints, detectedStack, source})` 输出 `ContextL2ProjectMap`。
  - 按顶层目录分组模块，模块自带 `name`/`path`/`responsibility`；只记录非 generated、非 sensitive 的文件。
  - 每个模块附带 `entrypoints`（与外部入口集合的交集）与 `tests`（`.spec.*`/`.test.*` 或 `__tests__/` 内的文件）。
  - `responsibility` 采用启发式描述：`apps` 应用入口、`packages` 共享包、`src` 源码、`docs` 文档、`scripts` 自动化脚本、其余通用“project module”。

