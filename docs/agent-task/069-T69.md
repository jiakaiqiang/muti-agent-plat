# T69 ContextEnvelopeV2 合同

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T68

## 目标

定义 L0 至 L6 分层 Context 合同。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 shared typecheck。

## 验收标准

- 各层字段可表达且版本固定为 v2。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/shared` 通过
  - `npm run build --workspace=@agent-cluster/shared` 通过
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
- 备注：
  - 在 `packages/shared/src/contracts.ts` 增加 `ContextEnvelopeV2Layer`（`'L0'`–`'L6'`）与 L0–L6 各层类型：
    - L0 `ContextL0WorkspaceIdentity`（workspaceId/rootName/providerKind/revision）
    - L1 `ContextL1NavigationManifest`（entries + truncated + nextCursor，条目携带 kind/generated/sensitive）
    - L2 `ContextL2ProjectMap`（source + modules + detectedStack）
    - L3 `ContextL3SelectedEvidence`（files 数组 + totalByteLength + truncated）
    - L4 `ContextL4ToolResults`（工具调用列表：tool/arguments/resultSummary/durationMs/succeeded）
    - L5 `ContextL5SummaryMemory`（bullets + turnCount）
    - L6 `ContextL6DeliveryArtifacts`（changeSetIds + reportIds）
  - `ContextEnvelopeV2` 顶层字段：`version:'v2'`、`createdAt`、`workspaceId`、`sessionId`、`L0`–`L6` 与 `budget`。
  - 新增 `packages/shared/src/context-envelope-v2.contract.test.ts`，用 `Assert<IsExact>` 断言层名与版本固定，并示例化 L0–L6 全部载荷。

