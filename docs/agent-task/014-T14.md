# T14 生成文件 Coverage

- 状态：completed
- 预计时间：10 分钟
- 阶段：B
- 依赖：T13

## 目标

在扫描覆盖率中记录生成文件跳过数量。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行 coverage 测试。

## 验收标准

- coverage 可审计 generatedSkipped。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`WorkspaceManifestCoverage` 新增必填 `generatedSkipped`，browser_local 与 server_local 扫描器在跳过共享生成目录时单独计数，Workspace Manifest 原样透传该审计字段。
- 验证：`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\orchestrator\\workspace-manifest.spec.ts .\\src\\common\\workspace-scanner.coverage.spec.ts`（在 `apps/server` 下执行，10 项通过）；`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\stores\\local-workspace-scanner.coverage.spec.ts`（在 `apps/web` 下执行，3 项通过）
- 备注：`generatedSkipped` 与 `skippedByReason.ignored_directory` 并存：前者专门审计生成目录数量，后者继续维持现有总跳过原因统计。
