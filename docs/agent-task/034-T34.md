# T34 Generic LLM 接入归一化

- 状态：completed
- 预计时间：15 分钟
- 阶段：E
- 依赖：T33

## 目标

在输出进入 Orchestrator 前标准化 Artifact。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 Generic LLM Runtime 单测。

## 验收标准

- 输出符合共享合同。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。GenericLlmRuntime 的 `artifacts()` 现在逐项调用 `normalizeRuntimeArtifact()`，在严格 RuntimeOutput Schema 校验与输出进入 Orchestrator 前完成旧正文迁移、旧类型映射和无效项过滤。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\generic-llm-runtime.service.spec.ts .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts .\src\modules\runtimes\runtime-output-schema.spec.ts`（在 `apps/server` 下执行，32 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：回归测试证明 `architecture_analysis + metadata.content` 的旧式 GLM 输出只需一次模型响应即可转为 `markdown + 顶层 content`，不会触发 schema repair。
