# T40 Post Review Actions 合同

- 状态：completed
- 预计时间：10 分钟
- 阶段：F
- 依赖：T39

## 目标

为 Post Review 定义结构化用户动作。

## 实施范围

apps/server/src/modules/orchestrator、sessions；前端 ConfirmationCard

## 测试

运行 shared 合同测试。

## 验收标准

- 可表达补读、受限交付、保存和取消。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：shared 新增 `PostReviewActionKey`、`PostReviewAction` 与 `POST_REVIEW_ACTION_KEYS`，以判别联合表达 `request_workspace_context`、`deliver_with_limitations`、`save_progress`、`cancel`；`PostReviewReportOutput.actions` 以可选字段兼容既有输出。
- 验证：先运行 `npm run typecheck --workspace @agent-cluster/shared` 得到预期合同红灯（缺少新导出和 actions 字段）；实现后该命令通过，`npx tsx --test packages/shared/src/post-review-actions.contract.test.ts` 1/1 通过，相关 `git diff --check` 通过（仅有既有行尾转换警告）。
- 备注：本任务仅定义合同；动作生成、Session 保留、前端渲染和确认分流分别留给 T41–T44。
