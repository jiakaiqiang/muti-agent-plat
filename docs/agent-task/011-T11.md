# T11 前端优先级扫描

- 状态：completed
- 预计时间：15 分钟
- 阶段：B
- 依赖：T10

## 目标

前端按文件价值而不是字母序读取正文。

## 实施范围

apps/web/src/stores/local-workspace-scanner.ts；apps/server/src/common/workspace-scanner.ts

## 测试

运行伪 DirectoryHandle 测试。

## 验收标准

- 80 文件额度下仍读到 src/main.ts。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。browser_local 扫描器先完整枚举可读候选，再按 `workspaceFileScanPriority()` 和路径稳定排序读取正文，避免普通文档提前耗尽 80 文件额度。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\stores\\local-workspace-scanner.priority.spec.ts .\\src\\stores\\local-workspace-scanner.coverage.spec.ts`（在 `apps/web` 下执行，5 项通过）；`npm run build -w @project/web`
- 备注：伪 `DirectoryHandle` 回归用例构造 80 个普通 Markdown 和 `src/main.ts`，确认入口文件仍进入正文；服务端排序留给 T12。
