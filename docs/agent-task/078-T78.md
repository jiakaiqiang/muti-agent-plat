# T78 消除 Context 重复清单

- 状态：pending
- 预计时间：10 分钟
- 阶段：J
- 依赖：T77

## 目标

禁止 Snapshot、Manifest、ProjectMap 重复携带文件列表。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 builder 单测。

## 验收标准

- Runtime 输入不存在重复全量清单。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/assert-no-duplicate-listings.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/assert-no-duplicate-listings.ts`：
    - `assertNoDuplicateFileListings(envelope)` 返回 `{ hasDuplicates, duplicatedPaths }`，比对 L1 Manifest 与 L2 ProjectMap.modules 的 `entrypoints`/`tests`、以及 L3 Evidence 的路径重叠。
    - `pruneDuplicatePathsFromProjectMap(envelope)` 从 L2 中剔除已被 L1 Manifest 收录的路径，L1 保持不变，保证 Runtime 输入不会同时携带同一批文件的“清单副本”。

