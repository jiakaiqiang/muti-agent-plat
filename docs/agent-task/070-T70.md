# T70 阶段 Context Policy

- 状态：pending
- 预计时间：10 分钟
- 阶段：J
- 依赖：T69

## 目标

定义 discussion、execution、review 等阶段策略。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 policy 单测。

## 验收标准

- 各阶段允许的 Context 层不同。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/phase-context-policy.spec.ts` 4/4 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/phase-context-policy.ts`，`ContextPhase = 'discussion'|'execution'|'post_review'|'delivery'`，导出 `allowedLayersForPhase` 与 `isLayerAllowedInPhase`。
  - 各阶段允许的 Context 层：discussion=L0/L1/L2/L5（讨论阶段不带 Evidence/工具结果/交付物）；execution=+L3/L4；post_review=+L6；delivery 只保留 L0 + L6 收敛到交付物。

