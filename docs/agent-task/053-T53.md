# T53 Server-local 路径归一化

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T52

## 目标

实现工作区根目录内的安全路径解析。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 path safety 单测。

## 验收标准

- 绝对路径和 ../ 被拒绝。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-path.spec.ts` 8/8 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-path.ts`，`resolveWorkspacePath(root, rel)` 返回 `{ absolute, relative }`；`''` 与 `.` 视为根目录，反斜杠归一化为 `/`。
  - 显式拒绝：POSIX 绝对路径、Windows 盘符绝对路径、`..` 上跳越界与拼接后越出 root 前缀四类情况，错误信息包含 `absolute` / `traversal` / `workspace` 关键词便于告警。
  - 现有 `apps/server/src/common/server-file-changes.ts` 与 `path-safety.ts#safeJoin` 保持不变；新 helper 面向 workspace-plane 使用（后续 T54+ 会作为 ServerLocal provider 的默认路径解析器）。

