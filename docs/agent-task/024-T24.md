# T24 实现 Evidence Floor

- 状态：completed
- 预计时间：15 分钟
- 阶段：D
- 依赖：T23

## 目标

调整裁剪顺序并保护当前任务证据。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 token-fit 测试。

## 验收标准

- 低价值上下文先于 Evidence 被删除。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。为确实携带源码/差异正文且需要 Grounding 的任务增加 Evidence Floor：常规裁剪阶段至少保留 1 条、96 字符正文，低价值事件、RAG、Artifact、Map、Tree 等仍按既有阶段先行收缩。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（在 `apps/server` 下执行，7 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：通过阶段 breakdown 定量校准下限；420-token 用例不再进入 `navigation_only` 且保留源码正文。真正无法容纳 Evidence 时仍允许最终降级为 `navigation_only`，其完成门禁由 T25 处理。
