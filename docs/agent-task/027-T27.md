# T27 Token 安全余量

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T26

## 目标

增加模型输入安全余量配置。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 runtime-config 单测。

## 验收标准

- 默认余量存在且可配置。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 `LLM_INPUT_SAFETY_MARGIN_RATIO`（默认 `0.1`，可配置为 `0`–`0.5`），并通过 `reserveInputTokenSafetyMargin()` 将模型输入上限折算为保留余量后的有效门槛；Orchestrator 使用有效门槛裁剪与拦截，同时仍把原始模型预算传给 Runtime。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\common\runtime-config.spec.ts .\src\common\token-fit.spec.ts .\src\modules\orchestrator\orchestrator.service.spec.ts`（在 `apps/server` 下执行，30 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：`TOKEN_CONTEXT_TRIMMED` 与 `TOKEN_BUDGET_EXCEEDED` 诊断现在同时记录 `maxInputTokens`、`effectiveMaxInputTokens`、`inputSafetyMarginTokens` 和 `inputSafetyMarginRatio`；示例配置已写入 `.env.example`。
