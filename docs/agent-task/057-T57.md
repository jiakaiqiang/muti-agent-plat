# T57 Server-local searchText

- 状态：pending
- 预计时间：15 分钟
- 阶段：H
- 依赖：T56

## 目标

实现受限文本搜索。

## 实施范围

apps/server/src/modules/workspaces；apps/server/src/common/server-file-changes.ts

## 测试

运行 tmpdir 单测。

## 验收标准

- 结果数量和 snippet 大小受限。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-search-text.spec.ts` 5/5 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-search-text.ts`，`searchServerLocalText` 返回 `SearchTextResult`。
  - 结果数受 `maxResults`（默认 100）限制并在超出时置 `truncated=true`；单个 preview 上限 240 字符，长行两侧用 `…` 收敛。
  - 递归时跳过 `GENERATED_WORKSPACE_DIRECTORIES` 与 `isSensitivePath` 命中的项；单文件超过 512KB 或含 `\0` 二进制探测即跳过；`caseSensitive` 默认关闭。

