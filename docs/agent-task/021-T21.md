# T21 Snapshot 文件上限测试

- 状态：completed
- 预计时间：10 分钟
- 阶段：D
- 依赖：T20

## 目标

为 Runtime Snapshot 元数据数量添加失败测试。

## 实施范围

apps/server/src/common/token.ts；Runtime/Orchestrator token 诊断

## 测试

运行 token-fit 测试。

## 验收标准

- 大 Snapshot 裁剪后文件数量受控。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增 80 文件 Runtime Snapshot 的 token-fit 失败测试，固定 initial 裁剪阶段必须限制 `workspaceSnapshot.files` 元数据对象数量。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\common\\token-fit.spec.ts`（在 `apps/server` 下执行）；修改前 6 项中 5 项通过、1 项按预期失败，compact 后仍有 80 个文件对象。
- 备注：本任务只建立失败测试，没有修改 `compactWorkspaceSnapshot()`；实际裁剪修复严格留给依赖任务 T22。
