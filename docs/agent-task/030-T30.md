# T30 严格 Artifact Schema

- 状态：completed
- 预计时间：10 分钟
- 阶段：E
- 依赖：T29

## 目标

定义 type、title、content 等严格结构。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 Schema 单测。

## 验收标准

- Artifact 类型枚举和必填字段生效。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。新增共享运行时常量 `ARTIFACT_TYPES` 并由其派生 `ArtifactType`；`RuntimeArtifactOutput` 和 `task_execution_result.changedArtifacts` JSON Schema 现在统一要求标准枚举 `type`、非空 `title` 与非空顶层 `content`，可选字段仅保留 `uri`、`summary` 和 `metadata`。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\runtime-output-schema.spec.ts .\src\modules\runtimes\codex-runtime-adapter.service.spec.ts .\src\modules\runtimes\claude-code-runtime-adapter.service.spec.ts .\src\modules\runtimes\test-runner-runtime-adapter.service.spec.ts .\src\modules\runtimes\streaming\frame-to-output.mapper.spec.ts`（在 `apps/server` 下执行，38 项通过）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：已同步补齐 Codex、Claude Code、Test Runner 与 streaming mapper 的 Artifact 正文，并更新 Runtime 合同；测试命令无 stdout/stderr 时会写入非空兜底正文。
