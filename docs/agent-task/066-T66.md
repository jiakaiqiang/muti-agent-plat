# T66 Index 推导测试关联

- 状态：pending
- 预计时间：15 分钟
- 阶段：I
- 依赖：T65

## 目标

关联源码与 spec/test 文件。

## 实施范围

apps/server/src/modules/workspaces/workspace-index；packages/shared 合同

## 测试

运行 Index 单测。

## 验收标准

- foo.ts 可关联对应测试。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/workspaces/workspace-index/associate-workspace-tests.spec.ts` 6/6 通过
- 备注：
  - 新增 `apps/server/src/modules/workspaces/workspace-index/associate-workspace-tests.ts`，`associateWorkspaceTests(entries)` 返回 `Map<sourcePath, testPaths>`（值内部去重排序）。
  - 支持 4 类测试文件：同目录 `foo.spec.ts` / `foo.test.ts`、`__tests__` 兄弟目录 `src/__tests__/service.test.ts`、`.tsx` 组件 `Button.test.tsx`、以及一个源文件对应多个测试的场景。
  - 源文件必须在索引里真实存在才会被关联；`.spec.ts`/`.test.ts` 自身不会被回指到自己。

