# T39 blocked 与 needs_review 分流

- 状态：completed
- 预计时间：15 分钟
- 阶段：F
- 依赖：T38

## 目标

按真实完成度处理两种状态。

## 实施范围

apps/server/src/modules/orchestrator、sessions；前端 ConfirmationCard

## 测试

运行 Orchestrator 单测。

## 验收标准

- blocked 等待，needs_review 才进入复核。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：Orchestrator 现按 `TaskExecutionResultOutput.status` 分流：`blocked` 固定映射为 `CONTEXT_INSUFFICIENT` 并进入 `waiting`，不会发出 `task_completed`；`needs_review` 保持完成语义并在完成事件中写入 `needsReview=true`。
- 验证：`npx tsx --test src/modules/orchestrator/orchestrator.service.spec.ts`（12/12 通过）；`npm run typecheck --workspace @agent-cluster/server`（通过）；相关 `git diff --check`（通过，仅有既有行尾转换警告）。
- 备注：是否携带 `requestedContext` 现在只影响补读重试，不再决定 blocked 与 failed 的业务语义。
