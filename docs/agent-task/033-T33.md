# T33 未知 Artifact 类型处理

- 状态：completed
- 预计时间：10 分钟
- 阶段：E
- 依赖：T32

## 目标

映射或拒绝 architecture_analysis 等未知类型。

## 实施范围

apps/server/src/modules/runtimes；packages/shared Artifact 合同

## 测试

运行 normalizer 单测。

## 验收标准

- 未知类型不再直接通过。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。`normalizeRuntimeArtifact()` 现在用共享 `ARTIFACT_TYPES` 做运行时枚举判定；旧类型 `architecture_analysis` 显式映射为标准 `markdown`，其他无映射未知类型返回 `undefined`，不再通过不安全类型断言进入合同。
- 验证：`node ..\..\node_modules\tsx\dist\cli.mjs --test .\src\modules\runtimes\runtime-artifact-normalizer.spec.ts .\src\modules\runtimes\runtime-output-schema.spec.ts`（在 `apps/server` 下执行，9 项通过）；`npm run typecheck -w @agent-cluster/shared`；`npm run typecheck -w @agent-cluster/server`
- 备注：未知类型策略集中在 `legacyArtifactTypeMap`；当前仅保留系统设计明确涉及的 `architecture_analysis → markdown` 兼容映射。
