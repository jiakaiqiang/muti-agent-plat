# T16 补充分析意图规则

- 状态：completed
- 预计时间：10 分钟
- 阶段：C
- 依赖：T15

## 目标

补充查看、介绍、熟悉、架构等分析表达。

## 实施范围

apps/server/src/modules/intent-recognition；apps/server/src/modules/orchestrator

## 测试

运行 IntentRecognition 单测。

## 验收标准

- 真实输入被识别为 analysis。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：已完成。扩展 `detectTaskIntent()` 的分析表达规则，覆盖“查看、介绍、熟悉、架构、梳理”等表述；精确输入“查看当前项目的架构”现识别为 `analysis`。
- 验证：`node ..\\..\\node_modules\\tsx\\dist\\cli.mjs --test .\\src\\modules\\intent-recognition\\intent-recognition.service.spec.ts`（在 `apps/server` 下执行，7 项通过）；`npm run typecheck -w @agent-cluster/server`
- 备注：更具体的交付、验证、review、排查和计划规则仍位于 analysis 分支之前，避免分析关键词抢占明确意图。
