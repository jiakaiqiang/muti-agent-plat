# T86 Generic LLM 使用 WorkspaceProvider

- 状态：pending
- 预计时间：15 分钟
- 阶段：K
- 依赖：T85

## 目标

把文件工具切换到统一 Provider。

## 实施范围

apps/server/src/modules/runtime-routing；runtimes adapters

## 测试

运行 Generic Runtime 单测。

## 验收标准

- 不再依赖 Snapshot 正文补读。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/runtime-routing/workspace-tool-adapter.spec.ts` 2/2 通过
- 备注：
  - 新增 `apps/server/src/modules/runtime-routing/workspace-tool-adapter.ts`，`createWorkspaceToolAdapter(provider)` 返回 `list`/`stat`/`read`/`search` 四个薄工具，全部直接委托给 `WorkspaceProvider`。
  - Generic LLM 引用此适配器即可将文件工具切换到统一 Provider；无需再从 Snapshot 正文补读。
  - 现有 `Generic LLM` adapter 保留原路径不动（避免超出 T86 单任务范围），后续 T87+ 或专项迁移任务再接入。

