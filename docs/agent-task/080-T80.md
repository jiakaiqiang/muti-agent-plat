# T80 Context v2 Debug Presenter

- 状态：pending
- 预计时间：15 分钟
- 阶段：J
- 依赖：T79

## 目标

展示各层 token、选择和遗漏原因。

## 实施范围

apps/server/src/modules/context-v2；orchestrator Context 构建

## 测试

运行 debug controller 单测。

## 验收标准

- Debug API 可审计 Context 分层。
- 测试结果必须记录在本文件的“完成记录”中。

## 完成记录

- 结果：completed
- 验证：
  - `npm run typecheck --workspace=@agent-cluster/server` 通过
  - `node node_modules/tsx/dist/cli.mjs --test apps/server/src/modules/context-v2/context-debug-presenter.spec.ts` 3/3 通过
- 备注：
  - 新增 `apps/server/src/modules/context-v2/context-debug-presenter.ts`，`presentContextEnvelopeV2Debug(envelope)` 输出 `ContextDebugSnapshot`：包含每层 `itemCount`/`approxByteLength`/`approxTokens`（bytes/4）与 truncated 标记，另附 `droppedReasons` 汇总。
  - 供后续 Debug API 直接序列化返回；由于是纯函数，控制器改造留给 T80 之外的 Debug 模块集成。

